services:
  gotenberg:
    image: gotenberg/gotenberg:8
    restart: always
    ports:
      - '127.0.0.1:3000:3000'
    networks:
      - csd_filling_station_network

networks:
  csd_filling_station_network:
    name: csd_filling_station_network
    driver: bridge
