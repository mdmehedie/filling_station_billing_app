services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - '8000:80'
      - '3210:3000'
    volumes:
      - .:/var/www/html

volumes:
  sail-mysql:
    driver: local
  sail-redis:
    driver: local
